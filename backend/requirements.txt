# Fas# LiveKit - basic versions for text chat
livekit==1.0.25
livekit-api==1.1.0
# livekit-agents>=0.12.20  # Not needed for direct TTS API calls
# livekit-plugins-silero==0.6.0  # Not needed for text chat
# livekit-plugins-noise-cancellation==0.2.5  # Removed due to version conflicts
# livekit-plugins-cartesia==0.4.12  # Using direct API calls instead
# livekit-plugins-groq==0.1.3  # Using groq package directly server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# LiveKit - basic versions for text chat
livekit==0.12.0
livekit-api==0.6.0
livekit-agents==0.8.0  # Required for TTS plugins
# livekit-plugins-silero==0.6.0  # Not needed for text chat
# livekit-plugins-noise-cancellation==0.2.5  # Removed due to version conflicts
livekit-plugins-cartesia==0.4.12  # Required for TTS
# livekit-plugins-groq==0.1.3  # Using groq package directly

# Groq
groq==0.4.2

# Database
sqlalchemy==2.0.25
alembic==1.13.1

# Environment and Configuration
# python-dotenv==1.0.0  # Not needed for text chat
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# Date and time
python-dateutil==2.8.2

# HTTP requests
httpx==0.26.0
aiohttp==3.9.1

# Audio processing
numpy==1.26.3

# Logging
structlog==24.1.0
